# This repo hosts avrmaestro.com and the AVR Maestro issue tracker.
# Only the served tree, the README and the issue templates belong here;
# everything else is ignored so nothing else can be added by accident.
/*
!/.gitignore
!/docs/
!/.github/
/.github/*
!/.github/ISSUE_TEMPLATE/
!/README.md
